export class AggData {

  organization: string;
  numerator: number;
  denominator: number;

  constructor(organization: string, numerator: number, denominator: number) {
    this.organization = organization;
    this.numerator = numerator;
    this.denominator = denominator;
  }

}
